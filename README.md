# Acceleration Program Application Automation Tracker

This tool generates the tracker of open task and proposal for internal use. 

## Usage

```bash
poetry install
poetry run python3 src/main.py   
```
